@model Player
@{
    ViewData["title"] = "Get Player";
}
<div class="text-end">
    <a asp-action="GetAllPlayers" class="btn btn-outline-primary mt-2 w-25">Back</a>
</div>
<h2 class="h3 mb-3 text-muted text-center fw-bold">Get Player By Id</h2>
<form class="mt-3" method="post">
    <div class="input-group">
        <input type="number" class="form-control" name="id" />
    </div> <br />
    <button class="btn btn-primary mt-2">Get Player</button>
</form>

@if (Model != null && TempData["success"] != null)
{
    <table class="table table-striped table-bordered text-center mt-3">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Position</th>
                <th>Nationality</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>@Model.Id</td>
                <td class="bg-warning">@Model.Name</td>
                <td class="bg-primary">@Model.Position</td>
                <td class="bg-info">@Model.Nationality</td>
            </tr>
        </tbody>
    </table>
}

@section Scripts{
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}